import styled from "styled-components";

export const DivCard = styled.div`
  background-image: linear-gradient(160deg, var(--blue) 0%, transparent 100%);
  display: flex;
  flex-direction: column;
  gap: 20px;
  margin: 20px auto;
  border-radius: 10px;
  padding: 40px;
  width: 500px;
  padding: 100px;
`;
